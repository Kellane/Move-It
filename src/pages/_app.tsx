
import '../styles/global.css'
import { ChallengesProvider } from '../contexts/ContextsChallenges'


function MyApp({ Component, pageProps }) {

  return (
      <Component {...pageProps} />
  )
}

export default MyApp
